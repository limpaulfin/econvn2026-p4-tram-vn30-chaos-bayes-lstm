# ECONVN2026 public demo — minimal dependency set for notebook + scripts.
# For the full paper pipeline (live vnstock data, pymc, seaborn, etc.) see the
# companion paper repository.

# Scientific stack
numpy>=1.26
pandas>=2.2
scipy>=1.12
pyarrow>=15.0

# Chaos indicators
nolds>=0.6.1
antropy>=0.1.7

# Deep learning (optional — notebook defaults RUN_LSTM=False)
torch>=2.2

# Plotting + notebook
matplotlib>=3.8
jupyter>=1.0
