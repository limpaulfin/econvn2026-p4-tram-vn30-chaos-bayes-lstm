# R visualizations (backup / verification figures).
# CUD palette for colorblind accessibility.

CUD <- c(black = "#000000", orange = "#E69F00", sky = "#56B4E9",
         green = "#009E73", yellow = "#F0E442", blue = "#0072B2",
         vermilion = "#D55E00", purple = "#CC79A7")

visualization_run <- function(params) {
  log_info("R figures: verification plots")
  # TODO WP3: recurrence plots + chaos concordance + diff vs Python
}
